runtime: ruby30
env: flex
service: default
entrypoint: bundle exec rails server -b 0.0.0.0 -p $PORT
runtime_config:
  operating_system: "ubuntu20.04"
#comment to check github Actions access the latest version.
