name: Build and Deploy RoR App to Google Artifact Registry

on:
  push:
    branches:
      - main 

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: ror-gcp  
      PROJECT_ID: ror_deployment
      REGION: asia-south1
      REPOSITORY_NAME: ror-gcp-docker
      SERVICE_NAME: GitHub-GCP-Credentials

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v0
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
        
      - name: Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v1
        with:
          service_account_key: ${{ secrets.GCP_SA_KEY }}
          project_id: ror-deployment
          export_default_credentials: true

      - name: Build Docker image
        run: |
          docker build -t $IMAGE_NAME:latest .

      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker $REGION-docker.pkg.dev --quiet

      - name: Push Docker image to Artifact Registry
        run: |
          docker tag ror-gcp asia-south1-docker.pkg.dev/ror_deployment/ror-gcp-docker/ror-gcp-ar:latest
          docker push asia-south1-docker.pkg.dev/ror_deployment/ror-gcp-docker/ror-gcp-ar:latest

